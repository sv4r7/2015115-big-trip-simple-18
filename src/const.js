const SortType = {
  DAY: 'day',
  PRICE: 'price',
};

const State = {
  DEFAULT: 'DEFAULT',
  EDITING: 'EDITING',
};

const Filter = {
  EVERYTHING: 'everything',
  FUTURE: 'future',
};

export { SortType, State, Filter };
